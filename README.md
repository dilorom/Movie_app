A modern and interactive **Movie App** built with **React**.  
This app allows users to browse, search, and view movie information fetched dynamically from an external movie database API.
📌 Features

- 🎞️ Browse popular movies  
- 🔍 Search for movies by title  
- 🧠 Displays movie details including poster, title, and rating  
- 💻 Responsive UI built with React components  
- ⚡ Smooth and dynamic interactivity using React state and hooks

---

## 🛠 Technologies Used

This project was built with:

- **React** – for building interactive UI  
- **JavaScript (ES6+)** – for logic and API interactions  
- **CSS / Tailwind CSS** – for styling and layout  
- **Fetch API / The Movie Database (TMDB) API** – for retrieving movie data  

---

## 📁 Installation

To run the app locally:

1. Clone the repository:
   ```bash
   git clone https://github.com/dilorom/Movie_app.git
Navigate into the project folder:

bash
Copy code
cd Movie_app
Install dependencies:

bash
Copy code
npm install
# or
yarn install
Start the development server:

bash
Copy code
npm start
# or
yarn start
Open http://localhost:3000 in your browser.

🧠 How It Works
Uses TMDB API to fetch movie data dynamically.

Displays movie posters and information using React components.

Users can search for movies in real time via the search bar.

State and hooks in React manage the UI and data flow efficiently.

📦 Folder Structure
csharp
Copy code
Movie_app/
├── public/             # Public assets (images, index.html)
├── src/
│   ├── components/     # React components
│   ├── pages/          # Pages / main views
│   ├── App.jsx         # Main React App
│   ├── index.js        # Entry point
│   └── styles/         # CSS or Tailwind files
├── package.json
└── README.md
